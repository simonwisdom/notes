<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }} - {% endif %}Notes | Simon Wisdom</title>
    <style>
        /* Base styles */
        :root {
            --text-color: #1a1a1a;
            --bg-color: #ffffff;
            --link-color: #2c5282;
            --meta-color: #666666;
            --border-color: #eee;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            line-height: 1.5;
            color: var(--text-color);
            background: var(--bg-color);
            max-width: 42rem;
            margin: 0 auto;
            padding: 2rem 1rem;
        }

        /* Typography */
        h1, h2, h3 {
            line-height: 1.2;
            margin: 0;
            font-weight: normal;
        }

        /* Links */
        a {
            color: var(--link-color);
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }

        /* Header */
        header {
            margin-bottom: 3rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid var(--border-color);
        }

        header h1 {
            margin: 0;
            font-size: 1.8rem;
        }

        nav {
            margin-top: 1rem;
        }

        nav a {
            margin-right: 1rem;
        }

        /* Posts */
        article {
            margin-bottom: 2.5rem;
        }

        article p {
            margin: 0 0 0.25rem 0;
        }

        .post-meta {
            font-size: 0.9rem;
            color: var(--meta-color);
            margin-bottom: 0.25rem;
        }

        .post-tags {
            font-size: 0.9rem;
            color: var(--meta-color);
        }

        /* Footer */
        footer {
            margin-top: 4rem;
            padding-top: 1rem;
            border-top: 1px solid var(--border-color);
            color: var(--meta-color);
            font-size: 0.9rem;
        }

        /* Add these styles to your existing CSS in base.njk */
        article h1, article h2, article h3 {
            margin-top: 1.5rem;
            margin-bottom: 1rem;
            font-weight: 600;
        }

        article ul, article ol {
            margin: 1rem 0;
            padding-left: 2rem;
        }

        article p {
            margin: 1rem 0;
        }

        article strong, article b {
            font-weight: 600;
        }

        article em, article i {
            font-style: italic;
        }

        article code {
            background: #f5f5f5;
            padding: 0.2em 0.4em;
            border-radius: 3px;
            font-family: monospace;
            font-size: 0.9em;
        }

        article pre {
            background: #f5f5f5;
            padding: 1rem;
            border-radius: 3px;
            overflow-x: auto;
        }

        article blockquote {
            border-left: 3px solid var(--border-color);
            margin: 1rem 0;
            padding-left: 1rem;
            color: var(--meta-color);
        }

        article img {
            max-width: 80%;
            height: auto;
            display: block;
            margin: 2rem auto;
        }

        article img + em {
            display: block;
            text-align: center;
            font-size: 0.9rem;
            color: var(--meta-color);
            margin-top: -1rem;
            margin-bottom: 2rem;
        }
    </style>
</head>
<body>
    <header>
        <nav>
            <a href="/">notes</a>
            <a href="/tags">tags</a>
        </nav>
    </header>

    <main>
        {{ content | safe }}
    </main>

    <footer>
        <p>&copy; {{ page.date.getFullYear() }} Simon Wisdom</p>
    </footer>
</body>
</html> 